# تعديلات صفحة OTP - OTP Timer Changes

## ملخص التغييرات

تم إضافة نظام مؤقت (Timer) متقدم لصفحة إدخال رمز التحقق (OTP) مع الميزات التالية:

## الميزات المضافة

### 1. **المؤقت الزمني (Timer)**
- عرض الوقت المتبقي بصيغة `MM:SS`
- بدء العد التنازلي من **دقيقتين (120 ثانية)**
- عند انتهاء الوقت، يتحول لون المؤقت إلى أحمر في آخر 30 ثانية
- أيقونة ساعة بجانب المؤقت

### 2. **رسالة انتهاء الصلاحية**
عند انتهاء الوقت يتم:
- عرض رسالة تحذير: "**انتهت صلاحية الرمز. يرجى طلب رمز جديد لإعادة المحاولة.**"
- تعطيل جميع حقول إدخال الأرقام (Disabled)
- منع إرسال الرمز المنتهي الصلاحية

### 3. **إعادة إرسال الرمز (Resend)**
- زر "إعادة إرسال الرمز" متاح فقط:
  - عند انتهاء صلاحية الرمز
  - أو عندما تقل الدقائق المتبقية عن 30 ثانية
- عند الضغط على الزر يتم:
  - إعادة تعيين المؤقت إلى دقيقتين
  - حذف جميع الأرقام المدخلة
  - إزالة رسالة الخطأ

### 4. **تحديث وصف الصفحة**
- تم تعديل النص ليقول: **"ستصلك رسالة نصية خلال دقيقتين"**

### 5. **التحقق المتقدم**
- لا يمكن إرسال الرمز إذا كان منتهي الصلاحية
- لا يمكن إرسال الرمز إذا لم يتم إدخال جميع الـ 6 أرقام

## التغييرات التقنية

### حالات جديدة (State)
```typescript
const [timeLeft, setTimeLeft] = useState(OTP_EXPIRY_SECONDS);
const [isExpired, setIsExpired] = useState(false);
```

### الثابت الزمني
```typescript
const OTP_EXPIRY_SECONDS = 120; // دقيقتان
```

### الدوال الجديدة
- `formatTime()` - لتحويل الثواني إلى صيغة `MM:SS`
- `resetTimer()` - لإعادة تعيين المؤقت والبيانات

### useEffect
- يقوم برصد المؤقت والعد التنازلي
- يعطل نفسه تلقائياً عند انتهاء الوقت

## واجهة المستخدم

### عرض المؤقت
```
🕐 الوقت المتبقي: 2:00
```

- يتحول اللون إلى الأحمر عندما يقل الوقت عن 30 ثانية
- يعرض الوقت بصيغة لحظية (يتحدث كل ثانية)

### رسالة الخطأ
```
⚠️  انتهت صلاحية الرمز. يرجى طلب رمز جديد لإعادة المحاولة.
```

### حالات الأزرار
- **زر تأكيد الرمز**: معطل عند انتهاء الصلاحية
- **زر إعادة الإرسال**: فعال عند انتهاء الصلاحية أو آخر 30 ثانية
- **زر الرجوع**: معطل عند التحميل

## الملف المعدل
- `src/components/otp-entry.tsx`

## الاستخدام

لا تحتاج إلى أي إعدادات إضافية. جميع الميزات تعمل تلقائياً عند الوصول إلى صفحة إدخال الرمز.

---

**تاريخ التحديث:** 16/7/2026  
**الإصدار:** 1.0
